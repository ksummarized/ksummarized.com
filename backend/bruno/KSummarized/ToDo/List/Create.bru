meta {
  name: Create
  type: http
  seq: 2
}

post {
  url: https://localhost:5000/api/todo/lists?name=demo
  body: none
  auth: bearer
}

params:query {
  name: demo
}

auth:bearer {
  token: {{token}}
}

body:json {
  "ToDo"
}

vars:post-response {
  listId: res.body.id
}
