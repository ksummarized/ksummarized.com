meta {
  name: Create
  type: http
  seq: 2
}

post {
  url: https://localhost:5000/api/todo/lists?name=kenobi
  body: none
  auth: bearer
}

params:query {
  name: kenobi
}

auth:bearer {
  token: {{token}}
}

body:json {
  "ToDo"
}
