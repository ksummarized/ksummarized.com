meta {
  name: Get
  type: http
  seq: 5
}

get {
  url: https://{{api_base_url}}/api/todo/lists/{{listId}}
  body: none
  auth: bearer
}

params:query {
  ~includeSubtasks: true
  ~completed: true
  ~page: 1
  ~pageSize: 2
  ~tag: 1
}

auth:bearer {
  token: {{token}}
}
