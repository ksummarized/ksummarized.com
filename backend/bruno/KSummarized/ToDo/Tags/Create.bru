meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: https://{{api_base_url}}/api/todo/tags
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "NewTag"
  }
}

vars:post-response {
  tagId: res.body.id
}
