meta {
  name: Update
  type: http
  seq: 4
}

put {
  url: https://{{api_base_url}}/api/todo/tags/{{tagId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "UpdatedTag"
  }
}
